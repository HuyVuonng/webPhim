const routes = {
    movie: '/movie',
    tv: '/tv',
    genre: '/genre/:slug',
    country: '/country/:slug',
    search: '/search/:slug',
    watch: '/watch/:idfilm',
    home: '/',
};

export default routes;
